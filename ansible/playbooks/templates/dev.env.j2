# Pro-Mata Dev Environment - Generated by Ansible
# Environment: {{ environment | default('dev') }}
# Generated: {{ ansible_date_time.iso8601 }}

# === ENVIRONMENT IDENTIFICATION ===
ENVIRONMENT={{ environment | default('dev') }}
NODE_ENV=production

# === DOMAIN CONFIGURATION ===  
DOMAIN_NAME=promata.com.br

# === APPLICATION ===
BACKEND_IMAGE={{ backend_image }}
FRONTEND_IMAGE={{ frontend_image }}
MIGRATION_IMAGE={{ migration_image | default('norohim/pro-mata-migration-dev:latest') }}
BACKEND_REPLICAS={{ backend_replicas | default(1) }}
FRONTEND_REPLICAS={{ frontend_replicas | default(1) }}

# === DATABASE ===  
POSTGRES_DB={{ postgres_db }}
POSTGRES_USER={{ postgres_user }}
POSTGRES_PASSWORD=password123
POSTGRES_REPLICA_USER=replicator
POSTGRES_REPLICA_PASSWORD=replica123

# === AUTHENTICATION ===
JWT_SECRET=jwt-secret-key
JWT_EXPIRES_IN=1h

# === SSL/TLS ===
LETSENCRYPT_EMAIL=admin@promata.com.br

# === TRAEFIK ===
TRAEFIK_AUTH_USERS=admin:$$2y$$10$$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi

# === MONITORING ===
MONITORING_ENABLED={{ monitoring_enabled | default(true) }}
GRAFANA_ADMIN_PASSWORD=admin
PROMETHEUS_RETENTION=7d

# === SCALING ===
REDIS_REPLICAS=1
PGBOUNCER_REPLICAS=1
POSTGRES_REPLICA_COUNT=0

# === DEVELOPMENT FLAGS ===
RUN_SEED=false

# === CLOUDFLARE (Optional) ===
CLOUDFLARE_API_TOKEN={{ cloudflare_api_token | default('') }}
CLOUDFLARE_ZONE_ID={{ cloudflare_zone_id | default('') }}
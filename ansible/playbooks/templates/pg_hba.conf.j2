# PostgreSQL Client Authentication Configuration for Pro-Mata
# Generated by Ansible

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust
local   all             postgres                                peer

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             10.0.0.0/8              scram-sha-256
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             192.168.0.0/16          scram-sha-256

# Pro-Mata application connections
host    {{ postgres_db }}    {{ postgres_user }}       10.0.0.0/8              scram-sha-256
host    {{ postgres_db }}    {{ postgres_user }}       172.16.0.0/12           scram-sha-256

# Replication connections
host    replication     {{ postgres_user }}       10.0.0.0/8              scram-sha-256
host    replication     {{ postgres_user }}       172.16.0.0/12           scram-sha-256

# PgAdmin connections
host    all             all             10.0.0.0/8              scram-sha-256

# Deny all other connections
host    all             all             0.0.0.0/0               reject
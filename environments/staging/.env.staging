# Staging Environment Configuration
# Pro-Mata - Azure East US 2

# Database Configuration
POSTGRES_USER=promata_staging
POSTGRES_PASSWORD=ProMataStagingPass2024!
POSTGRES_DB=promata_staging
DATABASE_URL=postgresql://promata_staging:ProMataStagingPass2024!@postgres:5432/promata_staging

# JWT Configuration
JWT_SECRET=ProMataStagingJWTSecret2024VerySecureAndLong!
JWT_EXPIRES_IN=2h

# API Configuration
API_PORT=8080
API_HOST=0.0.0.0
NODE_ENV=staging

# Frontend Configuration
VITE_API_URL=https://api-staging.promata.duckdns.org
VITE_APP_TITLE=Pro-Mata Staging
VITE_ENVIRONMENT=staging

# Azure Configuration
AZURE_REGION=eastus2
AZURE_RESOURCE_GROUP=rg-promata-staging
VM_SIZE=Standard_B2ms

# Docker Swarm Configuration
SWARM_ADVERTISE_ADDR=10.2.1.4
SWARM_MANAGER_TOKEN=
SWARM_WORKER_TOKEN=

# Domain Configuration
DOMAIN_NAME=staging.promata.duckdns.org
API_DOMAIN=api-staging.promata.duckdns.org

# Monitoring
ENABLE_MONITORING=true
LOG_LEVEL=info

# Email Configuration (Staging)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@staging.promata.duckdns.org

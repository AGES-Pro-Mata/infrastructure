# PgBouncer User List - Pro-Mata Dev
# Format: "username" "password_hash"

# Main application user
"promata" "SCRAM-SHA-256$4096:salt$hash"

# Monitoring user  
"monitoring" "md5hash"

# Admin user (same as main user for simplicity in dev)
"promata" "SCRAM-SHA-256$4096:salt$hash"